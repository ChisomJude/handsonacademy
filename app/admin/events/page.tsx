import Link from 'next/link'; import {redirect} from 'next/navigation'; import {ArrowLeft} from 'lucide-react'; import {createSupabaseServerClient} from '@/lib/supabase/server'; import {EventManager} from '@/components/admin/event-manager'; import type {AcademyEvent} from '@/lib/events';
export const metadata = {title: 'Events'};
export default async function EventsAdmin() { const supabase = await createSupabaseServerClient(); const {data: {user}} = await supabase.auth.getUser(); if (!user || user.app_metadata?.role !== 'admin') redirect('/admin'); const {data, error} = await supabase.from('events').select('*').order('created_at', {ascending: false}); return <section className="shell section"><Link href="/admin" style={{fontSize:13,color:'var(--muted)',display:'inline-flex',gap:7,alignItems:'center'}}><ArrowLeft size={15}/> Admin overview</Link><span className="eyebrow" style={{display:'block',marginTop:30}}>Event operations</span><h1>Bootcamps & webinars</h1><p className="lede">Create, update, publish, or remove events. Website registrations are stored safely and available to export.</p>{error ? <p role="alert" style={{color:'#b9462b'}}>{error.message}</p> : <EventManager initial={(data || []) as AcademyEvent[]}/>}</section>; }
