import Link from 'next/link';import {BookOpen,Flag,Megaphone,Users,ShieldCheck,ArrowRight} from 'lucide-react';import {createSupabaseServerClient} from '@/lib/supabase/server';export const metadata={title:'Admin'};
export default async function Admin(){let email:string|undefined,role:string|undefined;if(process.env.NEXT_PUBLIC_SUPABASE_URL&&process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY){const {data:{user}}=await (await createSupabaseServerClient()).auth.getUser();email=user?.email;role=(user?.app_metadata?.role as string|undefined)}if(!email)return <section className="shell section"><span className="eyebrow">Admin portal</span><h1>Sign in to continue.</h1><Link href="/login" className="btn btn-primary">Open login</Link></section>;if(role!=='admin')return <section className="shell section"><span className="eyebrow">Admin portal</span><h1>Admin access is restricted.</h1><p className="lede">Signed in as {email}. Assign the admin role from Supabase before exposing operational tools.</p><Link href="/dashboard" className="btn btn-secondary">Back to Learning Hub</Link></section>;const items:[[typeof BookOpen,string,string,string],[typeof Users,string,string,string],[typeof Flag,string,string,string],[typeof Megaphone,string,string,string]]=[[BookOpen,'Content library','Tracks and missions','/admin/content'],[Users,'Learners & applications','Member access and applications','/admin'],[Flag,'Submissions','Review learner evidence','/admin/submissions'],[Megaphone,'Announcements','Publish community updates','/admin']];return <section className="shell section"><span className="eyebrow">Operations</span><h1 style={{fontSize:'clamp(38px,6vw,64px)',letterSpacing:'-.07em'}}>Keep the learning loop healthy.</h1><p className="lede">Manage the platform from a focused operational workspace.</p><div className="grid admin-grid" style={{marginTop:38}}>{items.map(([Icon,title,description,href])=><Link href={href} className="card" style={{padding:23}} key={title}><Icon color="var(--brand)"/><h2 style={{fontSize:21,margin:'15px 0 7px'}}>{title}</h2><p className="lede" style={{fontSize:13}}>{description}</p><span className="btn btn-secondary">Open <ArrowRight size={15}/></span></Link>)}</div><div className="card" style={{padding:22,marginTop:30,display:'flex',gap:13,alignItems:'center'}}><ShieldCheck color="var(--brand)"/><p style={{fontSize:13,margin:0}}>Admin mutations are role-protected. Review and publishing actions will use server-side APIs.</p></div><style>{`.admin-grid{grid-template-columns:repeat(2,1fr)}@media(max-width:700px){.admin-grid{grid-template-columns:1fr}}`}</style></section>}
