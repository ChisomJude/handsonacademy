import Link from 'next/link';import {redirect} from 'next/navigation';import {ArrowLeft} from 'lucide-react';import {createSupabaseServerClient} from '@/lib/supabase/server';export const metadata={title:'Submission Review'};export default async function Submissions(){if(!process.env.NEXT_PUBLIC_SUPABASE_URL||!process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY)redirect('/admin');const supabase=await createSupabaseServerClient();const {data:{user}}=await supabase.auth.getUser();if(!user||user.app_metadata?.role!=='admin')redirect('/admin');const {data,error}=await supabase.from('submissions').select('id,mission_id,evidence,status,created_at,user_id').order('created_at',{ascending:false});return <section className="shell section"><Link href="/admin" style={{fontSize:13,color:'var(--muted)',display:'inline-flex',gap:7,alignItems:'center'}}><ArrowLeft size={15}/> Admin overview</Link><span className="eyebrow" style={{display:'block',marginTop:30}}>Review queue</span><h1>Submissions</h1><p className="lede">Review learner evidence and follow up with useful feedback.</p>{error?<p role="alert">{error.message}</p>:<div style={{display:'grid',gap:12,marginTop:30}}>{data?.length?data.map(item=><article className="card" style={{padding:20}} key={item.id}><div style={{display:'flex',justifyContent:'space-between',gap:12}}><b>{item.mission_id}</b><span className="tag">{item.status}</span></div><p style={{lineHeight:1.7,whiteSpace:'pre-wrap'}}>{item.evidence}</p><small style={{color:'var(--muted)'}}>Learner {item.user_id} · {new Date(item.created_at).toLocaleString()}</small></article>):<div className="card" style={{padding:24}}>No submissions yet. Learners will appear here after they complete a mission.</div>}</div>}</section>}
