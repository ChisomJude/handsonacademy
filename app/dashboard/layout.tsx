import Link from 'next/link';import {redirect} from 'next/navigation';import {createSupabaseServerClient} from '@/lib/supabase/server';import {DashboardNav} from '@/components/dashboard/dashboard-nav';
export default async function DashboardLayout({children}:{children:React.ReactNode}){let email:string|undefined;if(process.env.NEXT_PUBLIC_SUPABASE_URL&&process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY){const {data:{user}}=await (await createSupabaseServerClient()).auth.getUser();if(!user)redirect('/login?next=/dashboard');email=user.email}return <><div style={{height:72,borderBottom:'1px solid var(--line)',display:'flex',alignItems:'center',justifyContent:'space-between',padding:'0 24px',background:'#fff'}}><Link href="/dashboard" style={{fontWeight:800,fontSize:20,letterSpacing:'-.06em'}}>HandsOn<span style={{color:'var(--accent)'}}>.</span></Link><Link href="/" style={{fontSize:13,color:'var(--muted)'}}>Back to website</Link></div><div style={{display:'flex',alignItems:'stretch'}}><DashboardNav email={email}/><main style={{flex:1,minWidth:0}}>{children}</main></div></>}
