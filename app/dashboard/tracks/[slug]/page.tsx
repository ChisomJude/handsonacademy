/* eslint-disable react-hooks/immutability */
import {notFound} from 'next/navigation';import Link from 'next/link';import {ArrowLeft,ArrowRight,CheckCircle2,Circle,LockKeyhole,PlayCircle} from 'lucide-react';import {tracks} from '@/lib/tracks';import {allLessonMissions} from '@/lib/lesson-catalog';import {createSupabaseServerClient} from '@/lib/supabase/server';
export function generateStaticParams(){return tracks.filter(t=>t.available).map(t=>({slug:t.slug}))}
export default async function DashboardTrack({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const t=tracks.find(x=>x.slug===slug&&x.available);if(!t)return notFound();const supabase=await createSupabaseServerClient();const {data:{user}}=await supabase.auth.getUser();const {data:rows}=user?await supabase.from('mission_progress').select('mission_id,status').eq('user_id',user.id):{data:[]};const progress=new Map((rows||[]).map((r:{mission_id:string;status:string})=>[r.mission_id,r.status]));const trackMissions=allLessonMissions.filter(m=>m.track===slug).sort((a,b)=>a.number-b.number||a.id.localeCompare(b.id));let previousDone=true;return <div className="dash-content track-page"><Link href="/dashboard/tracks" className="back-link"><ArrowLeft size={15}/> My tracks</Link><div className="track-intro"><span className="tag">{t.category}</span><h1>{t.title}</h1><p className="lede">{t.project} · {t.duration}</p><p>{t.description}</p></div><div className="card track-note"><b>Recommended learning order</b><p>Complete each mission in order. Passing the knowledge check unlocks the next mission; completed missions remain open for review.</p></div><div className="track-list-heading"><h2>Guided missions</h2><div className="mission-legend"><span><PlayCircle size={15}/> Open</span><span><Circle size={15}/> In progress</span><span><CheckCircle2 size={15}/> Done</span><span><LockKeyhole size={15}/> Locked</span></div></div><div className="track-mission-list">{trackMissions.map((m,i)=>{const status=progress.get(m.id);const done=status==='completed';const inProgress=status==='in_progress';const open=previousDone;const locked=!open&&!done;if(open&&!done)previousDone=false;const Icon=done?CheckCircle2:locked?LockKeyhole:inProgress?Circle:PlayCircle;const content=<><Icon className="mission-icon" size={19}/><div><span className="mono mission-number">WEEK {m.week} · MISSION {String(i+1).padStart(2,'0')}</span><h3>{m.title}</h3><small>{m.time} · {m.milestone}</small><p className="mission-state">{done?'Completed':inProgress?'In progress':locked?'Locked: complete the previous mission':'Ready to open'}</p></div><ArrowRight size={17} className="mission-arrow"/></>;return locked?<div className="card track-mission is-locked" key={m.id}>{content}</div>:<Link href={`/dashboard/missions/${m.id}`} className={`card track-mission ${done?'is-done':''}`} key={m.id}>{content}</Link>})}</div><style>{`.track-page{padding:48px clamp(20px,5vw,60px);max-width:900px}.back-link{font-size:13px;color:var(--muted);display:inline-flex;gap:7px;align-items:center}.track-intro{margin-top:30px}.track-intro h1{font-size:clamp(34px,5vw,56px);letter-spacing:-.07em;margin:12px 0 6px}.track-intro>p:last-child{max-width:680px;line-height:1.7}.track-note{padding:23px;margin-top:32px}.track-note p{font-size:13px;color:var(--muted);line-height:1.7}.track-list-heading{display:flex;justify-content:space-between;align-items:center;gap:16px;margin-top:42px}.track-list-heading h2{margin:0}.mission-legend{display:flex;gap:12px;flex-wrap:wrap;color:var(--muted);font-size:11px}.mission-legend span{display:inline-flex;gap:5px;align-items:center}.track-mission-list{display:grid;gap:12px;margin-top:16px}.track-mission{padding:20px;display:flex;align-items:flex-start;gap:14px;text-decoration:none;color:inherit}.mission-icon{color:var(--brand);flex:0 0 auto;margin-top:2px}.track-mission.is-locked{opacity:.58;background:#f4f7f5}.track-mission.is-locked .mission-icon{color:var(--muted)}.track-mission.is-done{border-color:#82b9a3;background:#f5fbf7}.mission-number{font-size:10px;color:var(--accent)}.track-mission h3{margin:5px 0 2px;font-size:17px}.track-mission small{color:var(--muted)}.mission-state{margin:6px 0 0;font-size:12px;color:var(--muted)}.mission-arrow{margin-left:auto;color:var(--muted);flex:0 0 auto}@media(max-width:700px){.track-page{padding:32px 18px}.track-list-heading{align-items:start;flex-direction:column}.track-mission{padding:17px}.track-mission h3{font-size:16px}}`}</style></div>}
