import Link from 'next/link';import {ArrowRight} from 'lucide-react';import {tracks} from '@/lib/tracks';export const metadata={title:'My Tracks'};export default function MyTracks(){return <div className="dash-content"><span className="eyebrow">My tracks</span><h1 style={{fontSize:'clamp(34px,5vw,56px)',letterSpacing:'-.07em'}}>Choose where to build next.</h1><p className="lede">Your progress will live here as you complete missions and submit evidence.</p><div className="grid dash-track-grid" style={{marginTop:35}}>{tracks.filter(t=>t.available).map(t=><article className="card" style={{padding:24}} key={t.slug}><span className="tag">{t.category}</span><h2 style={{fontSize:25,letterSpacing:'-.05em'}}>{t.title}</h2><p className="lede" style={{fontSize:14}}>{t.description}</p><Link href={`/dashboard/tracks/${t.slug}`} className="btn btn-primary">Open learning path <ArrowRight size={16}/></Link></article>)}</div><style>{`.dash-content{padding:48px clamp(20px,5vw,60px);max-width:1200px}.dash-track-grid{grid-template-columns:repeat(2,1fr)}@media(max-width:700px){.dash-track-grid{grid-template-columns:1fr}.dash-content{padding:32px 18px}}`}</style></div>}
