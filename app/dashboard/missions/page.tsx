import Link from 'next/link';import {ArrowRight} from 'lucide-react';import {missions} from '@/lib/missions';export const metadata={title:'Missions'};export default function Missions(){return <div className="dash-content"><span className="eyebrow">Learning engine</span><h1 style={{fontSize:'clamp(34px,5vw,56px)',letterSpacing:'-.07em'}}>Missions to move you forward.</h1><p className="lede">Focused tasks, useful commands, and a clear definition of done.</p><div className="grid mission-grid" style={{marginTop:35}}>{missions.map(m=><article className="card" style={{padding:23}} key={m.id}><span className="tag">{m.track==='devops-beginner'?'DevOps':'Cloud Native'}</span><p className="mono" style={{fontSize:10,color:'var(--muted)',marginBottom:4}}>{m.milestone}</p><h2 style={{fontSize:23,letterSpacing:'-.05em',margin:'7px 0'}}>{m.title}</h2><p className="lede" style={{fontSize:13}}>{m.objective}</p><Link href={`/dashboard/missions/${m.id}`} className="btn btn-secondary">Open mission <ArrowRight size={16}/></Link></article>)}</div><style>{`.dash-content{padding:48px clamp(20px,5vw,60px);max-width:1100px}.mission-grid{grid-template-columns:repeat(2,1fr)}@media(max-width:700px){.mission-grid{grid-template-columns:1fr}.dash-content{padding:32px 18px}}`}</style></div>}
