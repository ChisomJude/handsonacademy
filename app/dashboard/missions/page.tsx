import Link from 'next/link';import {ArrowRight} from 'lucide-react';import {allLessonMissions} from '@/lib/lesson-catalog';export const metadata={title:'Missions'};export default function Missions(){const catalog=[...allLessonMissions].sort((a,b)=>a.track.localeCompare(b.track)||a.number-b.number);return <div className="dash-content"><span className="eyebrow">Learning engine</span><h1 style={{fontSize:'clamp(34px,5vw,56px)',letterSpacing:'-.07em'}}>Missions to move you forward.</h1><p className="lede">Learn → follow → practice → verify → challenge → submit.</p><div className="grid mission-grid" style={{marginTop:35}}>{catalog.map(m=><article className="card" style={{padding:23}} key={m.id}><span className="tag">{m.track==='devops-beginner'?'DevOps Beginner':'Docker 101'} · Week {m.week}</span><p className="mono" style={{fontSize:10,color:'var(--muted)',marginBottom:4}}>MISSION {String(m.number).padStart(2,'0')} · {m.time}</p><h2 style={{fontSize:23,letterSpacing:'-.05em',margin:'7px 0'}}>{m.title}</h2><p className="lede" style={{fontSize:13}}>{m.objective}</p><Link href={`/dashboard/missions/${m.id}`} className="btn btn-secondary">Open guided lesson <ArrowRight size={16}/></Link></article>)}</div><style>{`.dash-content{padding:48px clamp(20px,5vw,60px);max-width:1100px}.mission-grid{grid-template-columns:repeat(2,1fr)}@media(max-width:700px){.mission-grid{grid-template-columns:1fr}.dash-content{padding:32px 18px}}`}</style></div>}
