import {tracks} from "@/lib/tracks";import {TrackCard} from "@/components/tracks/track-card";import {Cta} from "@/components/marketing/sections";
export const metadata={title:"DevOps, Cloud & Cloud Native Learning Tracks",description:"Learn DevOps, cloud, and cloud native engineering by building real projects with guided missions and practical tasks."};
export default function Tracks(){return <><section className="shell section"><span className="eyebrow">Learning tracks</span><h1 style={{fontSize:'clamp(42px,6vw,68px)',letterSpacing:'-.07em',margin:'10px 0'}}>Your path starts with a project.</h1><p className="lede">Each track gives you a practical destination and the missions to get there.</p><div style={{display:'flex',gap:8,flexWrap:'wrap',marginTop:28}}>{['All','DevOps','Cloud','Cloud Native','SRE','Security','Development'].map(x=><span key={x} className="tag">{x}</span>)}</div></section><section className="shell" style={{paddingBottom:70}}><h2>Available now</h2><div className="grid cards">{tracks.filter(x=>x.available).map(x=><TrackCard key={x.slug} track={x}/>)}</div><h2 style={{marginTop:65}}>Coming soon</h2><p className="lede">Tell us what you want to build next.</p><div className="grid cards" style={{marginTop:25}}>{tracks.filter(x=>!x.available).map(x=><TrackCard key={x.slug} track={x}/>)}</div></section><Cta/><style>{`.cards{grid-template-columns:repeat(3,1fr)}@media(max-width:900px){.cards{grid-template-columns:repeat(2,1fr)}}@media(max-width:600px){.cards{grid-template-columns:1fr}}`}</style></>}
