import {NextResponse} from 'next/server';import {createSupabaseServerClient} from '@/lib/supabase/server';
export async function POST(request:Request){const supabase=await createSupabaseServerClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const body=await request.json() as {mission_id?:string;evidence?:string};if(!body.mission_id||!body.evidence?.trim())return NextResponse.json({error:'mission_id and evidence are required'},{status:400});const {data,error}=await supabase.from('submissions').insert({user_id:user.id,mission_id:body.mission_id,evidence:body.evidence.trim()}).select().single();if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({submission:data},{status:201});}
