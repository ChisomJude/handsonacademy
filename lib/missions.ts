export type Mission={id:string;track:string;milestone:string;title:string;objective:string;time:string;tasks:string[];commands:string[];expected:string;hint:string};
export const missions:Mission[]=[{id:'linux-shell-basics',track:'devops-beginner',milestone:'Prepare your Linux environment',title:'Navigate the terminal with confidence',objective:'Use the shell to inspect your environment, create a workspace, and document what you find.',time:'45 minutes',tasks:['Open a terminal session','Create a handson-workspace directory','Record your OS and shell versions','Write a short notes file'],commands:['mkdir -p ~/handson-workspace','uname -a','printf "Shell: %s\\n" "$SHELL"','touch ~/handson-workspace/notes.md'],expected:'A workspace directory containing notes.md and a short environment record.',hint:'If mkdir fails, check that your home directory is writable and that the path is spelled correctly.'},{id:'flask-health-route',track:'devops-beginner',milestone:'Build and run a Flask application',title:'Ship a health endpoint',objective:'Run a small Flask service and verify its response from the command line.',time:'60 minutes',tasks:['Create a virtual environment','Install Flask','Expose GET /health','Use curl to verify the response'],commands:['python3 -m venv .venv','source .venv/bin/activate','pip install flask','curl http://127.0.0.1:5000/health'],expected:'The endpoint responds with a successful status and a clear health message.',hint:'Activate the virtual environment before installing dependencies.'},{id:'docker-first-container',track:'cloud-native-beginner',milestone:'Hands-On Docker Basics',title:'Run your first container',objective:'Pull an image, run it locally, and inspect the container lifecycle.',time:'45 minutes',tasks:['Check Docker is available','Run an nginx container','List running containers','Stop and remove the container'],commands:['docker run -d --name hands-on-nginx -p 8080:80 nginx','docker ps','docker stop hands-on-nginx','docker rm hands-on-nginx'],expected:'The nginx welcome page is available at localhost:8080 while the container runs.',hint:'If port 8080 is busy, choose another local port such as 8081.'}];
