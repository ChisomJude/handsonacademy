import Link from "next/link";import {ArrowUpRight,Clock} from "lucide-react";import type {Track} from "@/lib/tracks";import {WaitlistButton} from "@/components/marketing/waitlist-modal";
export function TrackCard({track}:{track:Track}){return <article className="card" style={{padding:24,display:'flex',flexDirection:'column',gap:16}}><div style={{display:'flex',justifyContent:'space-between'}}><span className="tag">{track.category}</span><span className="mono" style={{fontSize:11,color:track.available?'#286166':'#ed6a45'}}>{track.available?'AVAILABLE':'COMING SOON'}</span></div><div><h3 style={{fontSize:23,letterSpacing:'-.05em',margin:'0 0 5px'}}>{track.title}</h3><b style={{fontSize:14}}>{track.project}</b></div><p className="lede" style={{fontSize:14,margin:0}}>{track.description}</p>{track.available&&<div style={{display:'flex',gap:10,fontSize:12,color:'#557078'}}><Clock size={16}/>{track.duration}</div>}<div style={{marginTop:'auto'}}>{track.available?<Link className="btn btn-primary" href={`/tracks/${track.slug}`}>Explore track <ArrowUpRight size={16}/></Link>:<WaitlistButton track={track.title}/>}</div></article>}
