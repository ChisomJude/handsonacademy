"use client";import {useState} from "react";
export function InterestForm({kind}:{kind:'sponsor'|'mentor'}){const [submitted,setSubmitted]=useState(false);const sponsor=kind==='sponsor';if(submitted)return <div className="card" style={{padding:30}}><span className="tag">Request received</span><h2>Thank you for supporting practical learning.</h2><p className="lede">This Phase 1 form is UI-only; no data has been stored yet. We’ll connect it to the backend before accepting live requests.</p></div>;return <form className="card" style={{padding:28}} onSubmit={e=>{e.preventDefault();setSubmitted(true)}}><div className="form-field"><label htmlFor="interest-name">{sponsor?'Your name':'Full name'}</label><input id="interest-name" required/></div><div className="form-field"><label htmlFor="interest-email">Work email</label><input id="interest-email" type="email" required/></div>{sponsor?<><div className="form-field"><label htmlFor="org">Organisation</label><input id="org" required/></div><div className="form-field"><label htmlFor="support">How would you like to support a bootcamp?</label><select id="support" required defaultValue=""><option value="" disabled>Select one</option><option>Fund a cohort</option><option>Provide equipment or cloud credits</option><option>Offer a venue or workspace</option><option>Explore a partnership</option></select></div></>:<><div className="form-field"><label htmlFor="role">Current role or area of expertise</label><input id="role" required/></div><div className="form-field"><label htmlFor="contribution">How would you like to contribute?</label><select id="contribution" required defaultValue=""><option value="" disabled>Select one</option><option>Mentor learners</option><option>Speak or run a workshop</option><option>Both</option></select></div></>}<div className="form-field"><label htmlFor="message">Tell us a little more</label><textarea id="message" rows={5} required/></div><button className="btn btn-primary">Send request</button></form>}
