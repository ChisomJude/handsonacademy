"use client";import {useState} from "react";export function ApplicationForm(){const [submitted,setSubmitted]=useState(false);if(submitted)return <div className="card" style={{padding:30}}><span className="tag">Application received</span><h2>Thanks for raising your hand.</h2><p className="lede">This Phase 1 form is UI-only; no application data has been stored. Add the community applications endpoint before inviting users to rely on submissions.</p></div>;return <form className="card" style={{padding:28}} onSubmit={e=>{e.preventDefault();setSubmitted(true)}}><div className="form-field"><label htmlFor="name">Full name</label><input id="name" required/></div><div className="form-field"><label htmlFor="email">Email</label><input id="email" type="email" required/></div><div className="form-field"><label htmlFor="country">Country</label><input id="country" required/></div><div className="form-field"><label htmlFor="experience">Experience level</label><select id="experience" required defaultValue=""><option value="" disabled>Select one</option><option>Beginner</option><option>Intermediate</option><option>Advanced</option></select></div><div className="form-field"><label htmlFor="interest">Career interest</label><select id="interest" required defaultValue=""><option value="" disabled>Select one</option>{['DevOps','Cloud','Cloud Native','SRE','Backend Engineering','Cybersecurity','Other'].map(x=><option key={x}>{x}</option>)}</select></div><div className="form-field"><label htmlFor="why">Why do you want to join?</label><textarea id="why" rows={5} required/></div><button className="btn btn-primary">Send application</button></form>}
