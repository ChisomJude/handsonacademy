"use client";import {useState} from 'react';import {Chrome,LoaderCircle} from 'lucide-react';import {createSupabaseBrowserClient} from '@/lib/supabase/browser';
export function LoginForm(){const [loading,setLoading]=useState(false),[error,setError]=useState('');async function login(){setLoading(true);setError('');if(!process.env.NEXT_PUBLIC_SUPABASE_URL||!process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY){setError('Authentication is not configured yet. Add the Supabase environment variables to enable login.');setLoading(false);return}const {error}=await createSupabaseBrowserClient().auth.signInWithOAuth({provider:'google',options:{redirectTo:`${window.location.origin}/auth/callback`}});if(error){setError(error.message);setLoading(false)}}return <div><button onClick={login} disabled={loading} className="btn btn-secondary" style={{width:'100%'}}>{loading?<LoaderCircle className="spin" size={18}/>:<Chrome size={18}/>} Continue with Google</button>{error&&<p role="alert" style={{color:'#b9462b',fontSize:13,lineHeight:1.5}}>{error}</p>}<style>{`.spin{animation:spin 1s linear infinite}@keyframes spin{to{transform:rotate(360deg)}}`}</style></div>}
