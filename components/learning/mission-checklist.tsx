"use client";import {useState} from 'react';import {CheckCircle2,Send} from 'lucide-react';import type {Mission} from '@/lib/missions';
export function MissionChecklist({mission}:{mission:Mission}){const [checks,setChecks]=useState<boolean[]>(mission.tasks.map(()=>false));const [sent,setSent]=useState(false);const complete=checks.every(Boolean);return <div><div style={{display:'grid',gap:10}}>{mission.tasks.map((task,i)=><label className="card" style={{padding:15,display:'flex',gap:11,alignItems:'center',cursor:'pointer'}} key={task}><input type="checkbox" checked={checks[i]} onChange={e=>setChecks(x=>x.map((v,n)=>n===i?e.target.checked:v))}/><span>{task}</span>{checks[i]&&<CheckCircle2 size={17} color="var(--brand)" style={{marginLeft:'auto'}}/>}</label>)}</div><div className="card" style={{padding:22,marginTop:25}}><span className="eyebrow">Challenge evidence</span><p style={{fontWeight:700}}>What did you build or learn?</p><textarea rows={5} placeholder="Paste a result, explain a decision, or link to evidence…" style={{width:'100%',font: 'inherit',padding:12,border:'1px solid var(--line)',borderRadius:8}}/><button disabled={!complete} className="btn btn-primary" style={{marginTop:14,opacity:complete?1:.5}} onClick={()=>setSent(true)}><Send size={16}/> {sent?'Submitted for review':'Mark mission complete'}</button>{sent&&<p role="status" style={{color:'var(--brand)',fontSize:13}}>Evidence captured in this preview. Persistence will be connected to submissions in the API.</p>}</div></div>}
