"use client";import {useState} from 'react';import {CheckCircle2,Send,LoaderCircle} from 'lucide-react';import type {Mission} from '@/lib/missions';import type {LessonMission} from '@/lib/lesson-content';import {KnowledgeGate} from '@/components/learning/knowledge-gate';
export function MissionChecklist({mission}:{mission:Mission|LessonMission}){const [checks,setChecks]=useState<boolean[]>(mission.tasks.map(()=>false));const [evidence,setEvidence]=useState('');const [quizPassed,setQuizPassed]=useState(false);const [sent,setSent]=useState(false),[saving,setSaving]=useState(false),[error,setError]=useState('');const complete=checks.every(Boolean);async function submit(){setSaving(true);setError('');const response=await fetch('/api/submissions',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({mission_id:mission.id,evidence})});if(!response.ok){setError('Sign in and complete the database setup before submitting evidence.');setSaving(false);return}const progress=await fetch('/api/progress',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({mission_id:mission.id,status:'completed'})});if(!progress.ok){setError('Evidence was saved, but progress could not be updated. Please try again.');setSaving(false);return}setSent(true);setSaving(false)}return <div><div style={{display:'grid',gap:10}}>{mission.tasks.map((task,i)=><label className="card" style={{padding:15,display:'flex',gap:11,alignItems:'center',cursor:'pointer'}} key={task}><input type="checkbox" checked={checks[i]} onChange={e=>setChecks(x=>x.map((v,n)=>n===i?e.target.checked:v))}/><span>{task}</span>{checks[i]&&<CheckCircle2 size={17} color="var(--brand)" style={{marginLeft:'auto'}}/>}</label>)}</div><KnowledgeGate track={mission.track} onPassed={setQuizPassed}/><div className="card" style={{padding:22,marginTop:25}}><span className="eyebrow">Challenge evidence</span><p style={{fontWeight:700}}>Submit only after completing the tasks and passing the knowledge check.</p><textarea value={evidence} onChange={e=>setEvidence(e.target.value)} rows={6} placeholder="Paste your command output, explanation, GitHub link, or other evidence here…" style={{width:'100%',font:'inherit',padding:12,border:'1px solid var(--line)',borderRadius:8}}/><button disabled={!complete||!quizPassed||!evidence.trim()||saving||sent} className="btn btn-primary" style={{marginTop:14,opacity:complete&&quizPassed&&evidence.trim()&&!saving&&!sent?1:.5}} onClick={submit}>{saving?<LoaderCircle className="spin" size={16}/>:<Send size={16}/>} {sent?'Mission submitted':saving?'Saving…':!quizPassed?'Pass the knowledge check first':'Complete mission'}</button>{error&&<p role="alert" style={{color:'#b9462b',fontSize:13}}>{error}</p>}{sent&&<p role="status" style={{color:'var(--brand)',fontSize:13}}>Evidence submitted for review.</p>}<style>{`.spin{animation:spin 1s linear infinite}@keyframes spin{to{transform:rotate(360deg)}}`}</style></div></div>}
