'use client';
import {useState} from 'react';
import {Check,Copy} from 'lucide-react';
export function CommandCard({command,explanation,expected}:{command:string;explanation:string;expected:string}){const [copied,setCopied]=useState(false);async function copy(){await navigator.clipboard.writeText(command);setCopied(true);setTimeout(()=>setCopied(false),1500)}return <div className="card" style={{padding:18,margin:'14px 0'}}><div style={{display:'flex',justifyContent:'space-between',gap:12,alignItems:'center'}}><span className="eyebrow">Command</span><button className="btn btn-secondary" onClick={copy} style={{padding:'7px 10px',fontSize:12}}>{copied?<Check size={14}/>:<Copy size={14}/>} {copied?'Copied':'Copy'}</button></div><pre style={{background:'var(--dark)',color:'#d6f1df',padding:15,borderRadius:8,overflowX:'auto',fontSize:13,lineHeight:1.7,margin:'12px 0'}}>{command}</pre><p style={{fontSize:13,lineHeight:1.6,margin:'8px 0'}}><b>What it does</b><br/>{explanation}</p><p style={{fontSize:13,lineHeight:1.6,margin:'8px 0',color:'var(--muted)'}}><b>Expected result</b><br/>{expected}</p></div>}
